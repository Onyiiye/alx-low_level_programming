#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @C: The charcater to print
 * 
 * Return: 0 success 1.
 * On error, -1 is returned, and error is set appropraiiatelyy
 */
int _putchar(char c)
{
  return (write(1, &c, 1));
}
